name: repository
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ">=0.1.0"

vars:
  model_name:
    type: string
    description: Your model name
    prompt: 'Enter the name of the model you want to create repo for:'
