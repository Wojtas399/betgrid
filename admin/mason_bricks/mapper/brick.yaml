name: mapper
description: A new brick created with the Mason CLI.

version: 0.1.0+1

environment:
  mason: ^0.1.0

vars:
  model_name:
    type: string
    description: Name of the model to map
    prompt: 'Enter the name of the model:'
